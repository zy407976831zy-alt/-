{
  "name": "youli-reality-image-studio",
  "version": "0.1.0",
  "private": true,
  "description": "游离现实影像生产台",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server/index.js && node --check server/providers/imageGenerator.js && node --check server/providers/referenceAnalyzer.js && node --check server/providers/qualityChecker.js && node --check web/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  }
}
